#include "Formatter.h"

namespace pluma {

void formatNode(const AstNode *nodePtr, const size_t indents) {
    if (nodePtr == nullptr) {
        return;
    }

    // TODO: formatter, finish it after the grammar is fixed.

    return;
}

void format(const Ast &ast) {}

}  // namespace pluma